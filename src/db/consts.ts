import path from "node:path";

export const ROOT = path.resolve(__dirname, "..", "..", "raw-input");
export const QUOTES_FILE = path.resolve(__dirname, "..", "..", "resources", "quotes.json");
export const ENCRYPTED_QUOTES_FILE = path.resolve(__dirname, "..", "..", "resources", "quotes.db");
